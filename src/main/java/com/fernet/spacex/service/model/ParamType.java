package com.fernet.spacex.service.model;

/**
 * Enum that represents the different types of query params that we can add to trello api.
 */
public enum ParamType {
    LIST,
    NAME,
    DESCRIPTION,
    MEMBERS, LABEL
}
